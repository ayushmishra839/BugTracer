{
  "name": "bugtracer",
  "version": "1.0.0",
  "scripts": {
    "build": "cd frontend && npm run build",
    "start": "cd frontend && npm start"
  },
  "devDependencies": {
    "@vercel/static-build": "^1.0.0"
  }
}
